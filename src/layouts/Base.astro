---
import Layout from './Layout.astro';

export interface Props {
  title: string;
  description?: string;
}

const { title, description = 'My personal blog' } = Astro.props;
---

<Layout title={title} description={description}>
  <slot />
</Layout>
